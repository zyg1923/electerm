# 打包与部署

这个仓库提交的是源码。依赖和编译结果不进 Git：它们能重新生成，而且 Windows 解包后的 `electerm.exe` 约 212MB，超过 GitHub 单文件 100MB 上限，普通 Git 提交会被拒绝。

## 环境

- Node.js 16 或更高
- Windows 打包需要在 Windows 上执行

## 安装依赖

```powershell
npm install
```

这会生成 `node_modules\`。没有这个目录时，`npm start` 和打包都会失败。不要把 `node_modules` 提交到 Git。

## 本地运行

```powershell
npm start
```

## 打 Windows 安装包

```powershell
npm run b
npx electron-builder --win nsis --x64
```

`npm run b` 会清理、编译前端，并把待打包文件准备好。安装包生成在 `dist\`。

## 部署

把 `dist\` 里的安装包拷到目标 Windows 机器安装。对方不需要安装 Node.js，也不需要本仓库的 `node_modules`。

## 未入库的文件

| 路径 | 是什么 | 没有它会怎样 | 怎么补 |
| --- | --- | --- | --- |
| `node_modules\` | npm 依赖 | 不能运行，也不能打包 | `npm install` |
| `dist\` | 安装包和 `win-unpacked\electerm.exe` | 仓库里没有现成的 exe | 按上面的步骤重新打包 |
| `work\` | 本地运行时拷出来的应用目录 | 不影响重新打包 | `npm start` 或 `npm run b` 会再生成 |
| 根目录 `electron-builder.json` | 打包时从 `build\electron-builder.json` 复制出来的配置 | 打包脚本会自己复制 | `npm run pb` 或走 `build\bin` 里的打包脚本 |

`build\` 目录里的脚本是源码，已经在 Git 里，不要删。
